'Imports Pandacap.Database

'Public Interface ICanonicalTagPostFilterService
'    Function FindRelevantTagsAsync(primaryTagId As Guid) As IAsyncEnumerable(Of Guid)

'    Function FilterToPossibleMatches(posts As IQueryable(Of Post),
'                                     tagIds As IEnumerable(Of Guid)) As IQueryable(Of Post)

'    Function IsMatch(post As Post,
'                     tagId As Guid) As Boolean
'End Interface
