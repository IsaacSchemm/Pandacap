@using Pandacap.Data
@using Pandacap.LowLevel
@using Pandacap.HighLevel
@using Pandacap.Models
@model FollowerViewModel

@{
    ViewBag.Title = "Followers";
}

@if (!Model.Items.Any())
{
    <p>
        There are no ActivityPub actors who follow this user.
    </p>
}
else
{
    @foreach (var item in Model.Items)
    {
        Uri uri = new(item.ActorId);

        <div class="mb-3">
            <div>
                <img height="50" src="@(item.IconUrl ?? "/images/trgray.svg")" alt="" />
                <span style="display: inline-block; vertical-align: top">
                    <strong>
                        @(item.PreferredUsername ?? item.ActorId)
                    </strong>
                    <br />
                    <a href="@item.ActorId">@item.ActorId</a>
                    @if (BridgyFed.OwnsInbox(item.SharedInbox ?? item.Inbox)
                        && !BridgyFed.Enabled)
                    {
                        <br />
                        <span class="small text-muted">
                            Bridgy Fed is currently disabled in the @UserAgentInformation.ApplicationName code and will not recieve new posts.
                        </span>
                    }
                </span>
            </div>
        </div>
    }
}
